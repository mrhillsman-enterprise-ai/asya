apiVersion: asya.sh/v1alpha1
kind: AsyncActor
metadata:
  name: test-timeout-cold
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "asya-test-actors.labels" . | nindent 4 }}
spec:
  actor: test-timeout-cold
  transport: {{ .Values.transport }}
  {{- include "asya-test-actors.pubsub-spec" . | nindent 2 }}
  flavors:
  - asya-test-actor

  resiliency:
    actorTimeout: "60s"

  scaling:
    enabled: true
    minReplicaCount: 0
    maxReplicaCount: 5
    pollingInterval: 5
    cooldownPeriod: 60
    queueLength: 1

  image: {{ .Values.workload.image }}
  imagePullPolicy: {{ .Values.workload.imagePullPolicy }}
  handler: asya_testing.handlers.payload.timeout_handler
